#@bot.event
#async def on_message(message):
#  if message.author == bot.user:
#    return

#  msg = message.content

#  if msg.startswith('!d bump'):
#    await message.channel.send(account.bump(msg.author.id))
